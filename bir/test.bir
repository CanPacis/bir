add [a, b] {
  init {
    let index = 4
  }

  index++
  return a + b + index
}

let c = add (2, 5)
let d = add (2, 5)
let e = add (2, 5)
let f = add (2, 5)

toplam implements add

let g = toplam (10, 20)
let h = toplam (10, 20)

implementor_engine_interface ()