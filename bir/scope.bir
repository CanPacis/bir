use "std"

scope:verb [index, value] {
  switch verb {
    case push {
      return bir:push (index, value)
    }
    case read {
      return bir:read (index)
    }
    default {
      throw unknown
    }
  }
}