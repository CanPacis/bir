use "std"

scope:verb [index, value] {
  switch verb {
    case write {
      return bir:write (index, value)
    }
    case read {
      return bir:read (index)
    }
    case delete {
      return bir:delete (index)
    }
    default {
      throw unknown
    }
  }
}